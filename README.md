# Live Markdown Editor

A Nuxt (Vite) app with a split-pane live markdown editor: write on the left, preview on the right.

## Setup

```bash
npm install
```

## Development

Start the dev server on `http://localhost:3000`:

```bash
npm run dev
```

## Production

```bash
npm run build
npm run preview
```
